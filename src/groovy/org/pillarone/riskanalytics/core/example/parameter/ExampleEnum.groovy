package org.pillarone.riskanalytics.core.example.parameter

/**
 * An example enum, which can be used in test cases to where enum parameters are required.
 */
enum ExampleEnum {
    FIRST_VALUE, SECOND_VALUE
}